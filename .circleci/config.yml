version: 2
jobs:
  build:
    docker:
      - image: debian:stretch

    steps:
      - checkout

      - run:
          name: Greeting
          command: echo Hello, world.
      - run:
          name: run tests
          command: gem install bundler && bundle && bundle exec rspec spec
      - run:
          name: Print the Current Time
          command: date
