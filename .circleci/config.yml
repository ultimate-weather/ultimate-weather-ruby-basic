version: 2
jobs:
  build:
    docker:
      - image: debian:stretch

    steps:
      - checkout

      - run:
          name: Greeting
          command: echo Hello, world.

      - run:
          name: run tests
          command: bndle exec rspec spec

      - run:
          name: Print the Current Time
          command: date
